# Identity candidates

Unapproved visual explorations belong here. Candidate files are never consumed
by generated public packages until a maintainer records an explicit decision in
`.identity/governance/approvals.json` and promotes the selected bytes into
`.identity/sources/`.
